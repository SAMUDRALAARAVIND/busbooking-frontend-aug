const baseUrl = "http://localhost:8000";

// TODO: add REST endpoints int this
const Endpoints = {
  CityData : `${baseUrl}/city/cities`,
  tripsList: `${baseUrl}/api/trips/details`,
};

export default Endpoints;
